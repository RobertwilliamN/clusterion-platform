apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: monitoring
spec:
  ingressClassName: nginx
  rules:
  - host: grafana.clusterion.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: observability-grafana
            port:
              number: 80
